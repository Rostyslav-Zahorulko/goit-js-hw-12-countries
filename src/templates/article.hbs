{{#each this}}
<h2>{{name}}</h2>
<p>Capital: {{capital}}</p>
<p>Population: {{population}}</p>
<ul>Languages:
    {{#each languages}}
    <li>{{name}}</li>
    {{/each}}
</ul>
<img src="{{flag}}" alt="flag" width="240px">
{{/each}}